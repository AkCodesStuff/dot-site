# Canonical origin for canonical URLs, Open Graph tags, sitemap.xml and robots.txt.
# No trailing slash. Set this per environment (local / preview / production).
NEXT_PUBLIC_SITE_URL=http://localhost:3000
